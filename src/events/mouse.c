#include "../../minirt.h"

// int handle_mouse_movements(int x, int y, t_rt *rt)
// {
// 	char *buffer = "Hello there";  // Buffer to hold our info string

// 	/* Format the string with the current mouse coordinates */
// //	snprintf(buffer, sizeof(buffer), "Mouse Position: x = %d, y = %d", x, y);
// 	/* Display the string in white (0xFFFFFF) at position (10,10) */
// 	mlx_string_put(rt->mlx, rt->win, x, y, 0xFFFFFF, buffer);
// 	return (0);
// }